# V4 压测报告

本文件由执行 `npm run seed:load-test` 和 `npm run load-test` 后填写真实结果。禁止在未运行目标环境压测时伪造数据。

| 项目 | 结果 |
|---|---|
| 测试时间 | 待执行 |
| 部署环境 | 待执行 |
| 数据库 | Neon PostgreSQL，HTTP 驱动 |
| 队列 | QStash，Parse 并发 2 / Batch 并发 4 |
| SKU 主数据 | 20,000 |
| Excel 行数 | 10,000 |
| 上传接口 P95 | 待执行 |
| 全链路耗时 | 待执行 |
| 批次 P50 / P95 | 待执行 |
| SKU 校验耗时 | 待执行 |
| 数据库写入耗时 | 待执行 |
| 行级错误率 | 待执行 |
| 500 / 504 | 待执行 |
| 是否 ≤60 秒 | 待执行 |

压测命令会输出 JSON 结果并在不达标时返回非零退出码。监控截图和 Neon 连接指标应在目标 Vercel 环境执行后附于本文件。

## 历史本地冒烟结果

2026-08-05 使用 Neon 实库和本地无签名 Worker 模式完成 2 行异步集成测试，测试结束后已精确清理任务、运单、SKU、Outbox、Trace 和性能日志。

| 项目 | 结果 |
|---|---:|
| 暖请求重复上传响应 | 0.89 秒 |
| 规则解析 | 4 ms |
| 规则执行 | 1 ms |
| 批量校验 | 675 ms |
| 旧版写入观测值 | 212 ms（旧实现只覆盖进度事务，不能代表真实运单写入） |
| 批次总耗时 | 2,835 ms |
| 聚合验证 | 2 个 SKU 行聚合为 1 个出库单，总数量 5 |
| 历史记录 | `shipment_rows / shipment_orders` 可查询 |
| Trace / 性能看板 | 可查询，监控聚合接口 HTTP 200 |

该结果只证明小批量链路正确性，不替代 10,000 行正式压测。复核后已修正 `insert_duration_ms` 的计时边界，使其覆盖真实 `shipment_orders / shipment_rows` 集合写入；因此旧版 212 ms 不作为当前性能证据，必须用最新代码重新执行。当前本地未配置 QStash，10,000 行吞吐、并发队列和生产 P95 仍标记为待执行。
